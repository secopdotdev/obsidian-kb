apiVersion: v1
kind: Service
metadata:
  name: mission-control
  namespace: mission-control
  labels:
    app.kubernetes.io/name: mission-control
spec:
  selector:
    app.kubernetes.io/name: mission-control
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
