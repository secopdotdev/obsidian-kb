# Concept Groups

> The concept groups are the primary organizing axis. The five below are an **editable
> example taxonomy** — rename, remove, or extend them in the group map (5 code sites; see
> the plugin README "Tier-2" note). Folders keep numeric slugs for sort/grep; the classifier
> is the display name.

| Slug | Classifier | Emoji |
|---|---|---|
| `1.0-dev` | Launchpad | 🚀 |
| `1.1-dev-tools` | Tool Bay | 🛠️ |
| `2.0-career` | Trajectory | 📈 |
| `3.0-work` | Mission Ops | 🛰️ |
| `5.0-home` | Ground Control | 🏡 |

Each group's projects live under `02-projects/<slug>/`. Add a per-group MOC note here as
your vault grows.
